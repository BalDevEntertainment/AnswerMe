<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="text_item_detail_title" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/font_size_small</item>
        <item name="android:textColor">@android:color/secondary_text_dark</item>
        <item name="android:layout_marginBottom">@dimen/spacing_small</item>
    </style>

    <style name="text_item_detail_value" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/font_size_medium</item>
        <item name="android:textColor">@android:color/primary_text_dark</item>
        <item name="android:layout_marginBottom">@dimen/spacing_normal</item>
    </style>

    <style name="edit_text_multiline">
        <item name="android:gravity">top|left</item>
        <item name="android:textSize">@dimen/font_body</item>
        <item name="android:inputType">textMultiLine</item>
        <item name="android:lines">8</item>
        <item name="android:maxLines">6</item>
        <item name="android:minLines">6</item>
        <item name="android:maxLength">4096</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
    </style>

</resources>
